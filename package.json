{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "dotenv": "^16.0.3",
    "hardhat": "^2.14.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "ganache": "ganache -m 'window diesel repeat rule host cream fly liquid step stumble step truly'"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1"
  }
}
